# 2026-07-22

The crashing when unfocusing bug is appearently solved:

when performing frame end operations, these should fall outside of the
*should render* condition, so that it is done anyways.

```cpp
        // shouldRender block
    }
    // End the frame
    XrFrameEndInfo frameEndInfo{ XR_TYPE_FRAME_END_INFO };
    frameEndInfo.displayTime = frameState.predictedDisplayTime;
    frameEndInfo.environmentBlendMode = XR_ENVIRONMENT_BLEND_MODE_OPAQUE;
    frameEndInfo.layerCount = static_cast<uint32_t>(layers.size());
    frameEndInfo.layers = layers.data();
    xrEndFrame(m_session, &frameEndInfo);
    return true;
}
```
