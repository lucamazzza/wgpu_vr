# 2026-07-18

After discussion with the relator and some thinking, the solution achievable is to take on the `wgpu-native` route and
introduce the missing extension to make everything (hopefully) work.

I also thought that, instead of modifying the source fo `WebGPU`, I could "insert" the extensions in the extracted
`Vulkan` instance/device but here the API prevents us; `Vulkan` is in fact zero-runtime-overhead, so it is impossibile
to modify the resources once instanced.


